import styled from 'styled-components';

export const LinkAlike = styled.button`

    cursor: pointer;
    border: none;
    background: rgba(0,0,0,0);
    padding: 0;

`

export const List = styled.ul`

    list-style-type: none;

`